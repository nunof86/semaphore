name: devops

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Show runner info
        run: |
          hostname
          whoami
          uname -a
          docker --version || true
          curl --version
